[alias]
  co-pr = !sh -c 'git fetch origin pull/$1/head:pr/$1 && git checkout pr/$1' -
  st = status
  s = switch
  cp = cherry-pick
  cpc = cherry-pick --continue
  cps = cherry-pick --skip
  cpa = cherry-pick --abort
  rbc = rebase --continue
  rbs = rebase --skip
  rba = rebase --abort
  ras = restore . --staged
  rsh = reset --hard
  puh = push -u origin head
  wip = commit -am WIP --no-verify
  amend = commit --amend --no-edit
[user]
  name = Oskar Kwaśniewski
  email = oskar@okwasniewski.com

  signingkey = ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKqrK1yGDIOmwlEDVDll8zIA4/kWJUIxmjhANBdYFbZP
[push]
  autoSetupRemote = true
[core]
  pager = delta
  editor = nvim

[interactive]
  diffFilter = delta --color-only
[add.interactive]
  useBuiltin = false # required for git 2.37.0

[delta]
  navigate = true    # use n and N to move between diff sections
  side-by-side = true
  light = false      # set to true if you're in a terminal w/ a light background color (e.g. the default macOS terminal)
  syntax-theme = Dracula

[merge]
  tool = nvim
[mergetool "nvim"]
  cmd = nvim -c "CodeDiff"
[mergetool]
  prompt = false

[diff]
  tool = nvim
  colorMoved = default

[difftool "nvim"]
  cmd = nvim -c "CodeDiff file $LOCAL $REMOTE"

[difftool]
  prompt = false

[alias]
  lo = log --oneline
  tree = log --oneline --graph --decorate --all

[gpg]
  program = /opt/homebrew/bin/gpg
	format = ssh
[rerere]
  enabled = true
  autoUpdate = true
[help]
	autocorrect = 1
[gpg "ssh"]
	program = /Applications/1Password.app/Contents/MacOS/op-ssh-sign
[commit]
	gpgsign = true
[init]
	defaultBranch = main
