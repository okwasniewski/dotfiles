# Pinned projects for hproj: name<TAB>path<TAB>startup command
# Startup command is optional; leave it empty for a plain shell.
dotfiles	~/dotfiles	nvim
obsidian	~/Personal	nvim
nvim-config	~/.config/nvim	nvim
downloads	~/Downloads	yazi
